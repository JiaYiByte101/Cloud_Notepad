# 群聊消息提醒功能说明

## 功能概述

为云记事本应用新增了群聊消息提醒功能，用户现在可以在页面右上角看到群聊消息的红色气泡提醒，点击可直接跳转到群聊消息列表。

## 新增功能

### 1. 导航栏群聊消息提醒按钮
- **位置**: 页面右上角导航栏，位于私聊消息按钮旁边
- **图标**: 使用 `bi-chat-square-dots` 图标，区别于私聊的 `bi-chat-dots` 图标
- **功能**: 显示群聊未读消息数量的红色气泡提醒
- **跳转**: 点击直接跳转到群聊列表页面

### 2. 实时消息数量统计
- **统计范围**: 统计用户参与的所有活跃群聊中的未读消息
- **更新频率**: 每30秒自动更新一次
- **触发更新**: 页面重新获得焦点时也会更新

### 3. 视觉优化
- **图标统一**: 将群聊列表页面的图标统一为Bootstrap Icons
- **用户体验**: 添加了tooltip提示，鼠标悬停显示"群聊消息"
- **响应式设计**: 适配不同屏幕尺寸

## 技术实现

### 后端修改

#### 1. 视图函数更新 (`friends/views.py`)
```python
def get_notification_counts(user):
    # 原有的好友请求和私聊消息统计
    pending_requests_count = FriendRequest.objects.filter(receiver=user, status='pending').count()
    unread_messages_count = Message.objects.filter(receiver=user, is_read=False).count()
    
    # 新增：群聊未读消息统计
    user_groups = ChatGroup.objects.filter(
        members__user=user,
        members__is_active=True,
        is_active=True
    )
    
    unread_group_messages_count = 0
    for group in user_groups:
        unread_count = group.messages.exclude(read_by=user).count()
        unread_group_messages_count += unread_count
    
    return {
        'pending_requests_count': pending_requests_count,
        'unread_messages_count': unread_messages_count,
        'unread_group_messages_count': unread_group_messages_count,  # 新增
        'total_count': pending_requests_count + unread_messages_count + unread_group_messages_count
    }
```

### 前端修改

#### 1. 导航栏模板更新 (`templates/includes/navbar.html`)
- 添加了群聊消息提醒按钮
- 更新了JavaScript代码以处理群聊消息数量显示

#### 2. 群聊列表页面优化 (`templates/friends/group_list.html`)
- 统一使用Bootstrap Icons
- 添加了实时更新功能的JavaScript框架

#### 3. 主页功能介绍更新 (`templates/home.html`)
- 在好友聊天功能介绍中添加了群聊功能说明
- 新增群聊消息按钮链接

## 使用说明

### 用户操作流程
1. **查看提醒**: 登录后在页面右上角可以看到群聊消息提醒按钮
2. **红色气泡**: 当有未读群聊消息时，按钮上会显示红色气泡和数字
3. **快速跳转**: 点击按钮直接跳转到群聊列表页面
4. **查看详情**: 在群聊列表中可以看到每个群聊的未读消息数量
5. **进入聊天**: 点击具体群聊进入聊天详情页面

### 消息已读机制
- 进入群聊详情页面时，所有消息会自动标记为已读
- 已读状态通过 `MessageReadStatus` 模型记录
- 用户的最后阅读时间会更新到 `ChatGroupMember.last_read_at`

## 注意事项

1. **权限控制**: 只有群聊成员才能看到该群聊的消息
2. **性能优化**: 未读消息统计使用了数据库查询优化
3. **实时更新**: 消息数量每30秒更新一次，避免频繁请求
4. **兼容性**: 保持了与现有私聊消息提醒功能的一致性

## 后续优化建议

1. **WebSocket集成**: 可以考虑使用WebSocket实现真正的实时消息推送
2. **消息预览**: 在提醒中显示最新消息的预览内容
3. **声音提醒**: 添加新消息的声音提醒功能
4. **桌面通知**: 集成浏览器的桌面通知API

## 问题修复

### 群聊消息已读状态不更新的问题

**问题描述**: 
1. 用户进入群聊详情页面后，导航栏的红色气泡提醒不会消除
2. **群聊列表页面中每个群聊项目的红色数字气泡（如"19"）在消息已读后不会消除**

**原因分析**: 
1. 消息已读状态的查询逻辑可能不够精确
2. 已读状态标记可能存在数据不一致的情况
3. **群聊列表页面的未读消息统计使用了错误的查询逻辑**

**解决方案**:

#### 1. 优化未读消息统计逻辑
```python
def get_user_unread_group_messages_count(user):
    """获取用户的群聊未读消息数量，使用更精确的查询"""
    user_groups = ChatGroup.objects.filter(
        members__user=user,
        members__is_active=True,
        is_active=True
    )
    
    total_unread = 0
    for group in user_groups:
        unread_count = GroupMessage.objects.filter(
            group=group
        ).exclude(
            messagereadstatus__user=user  # 使用反向关系查询
        ).count()
        total_unread += unread_count
    
    return total_unread
```

#### 2. 增强消息已读标记逻辑
- 在群聊详情页面确保所有消息都被正确标记为已读
- 在AJAX获取新消息时同步更新最后阅读时间
- 添加数据一致性检查

#### 3. 修复群聊列表页面的未读消息统计
```python
@login_required
def group_list(request):
    """显示用户所在的群聊列表"""
    user_groups = ChatGroup.objects.filter(
        members__user=request.user,
        members__is_active=True,
        is_active=True
    ).order_by('-updated_at')
    
    # 为每个群聊计算未读消息数量
    groups_with_unread = []
    for group in user_groups:
        # 使用正确的查询逻辑
        unread_count = GroupMessage.objects.filter(
            group=group
        ).exclude(
            messagereadstatus__user=request.user
        ).count()
        
        group.unread_count = unread_count
        groups_with_unread.append(group)
    
    return render(request, 'friends/group_list.html', {'groups': groups_with_unread})
```

#### 4. 添加实时更新功能
- 新增 `get_group_unread_counts_ajax` 接口用于实时获取所有群聊的未读消息数
- 群聊列表页面每3秒自动更新未读消息数显示
- 支持动态添加/移除红色数字气泡

#### 5. 添加调试功能
- 新增 `refresh_group_read_status` 视图用于手动刷新已读状态
- 在群聊列表页面添加"刷新已读状态"按钮
- 提供AJAX接口用于批量修复已读状态

### 新增功能

#### 手动刷新已读状态
- **位置**: 群聊列表页面右上角
- **功能**: 手动标记所有群聊消息为已读，解决数据不一致问题
- **使用场景**: 当红色气泡提醒不消除时，可以手动刷新

## 测试建议

1. 创建测试群聊并发送消息
2. 验证未读消息数量显示是否正确
3. 测试点击跳转功能
4. 验证消息已读后数量是否正确更新
5. 测试多个群聊的消息统计是否准确
6. **新增**: 测试"刷新已读状态"功能是否能解决红色气泡不消除的问题
7. **新增**: 验证进入群聊详情页面后，导航栏提醒是否立即更新 